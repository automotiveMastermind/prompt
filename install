rm -rRf backup
mkdir -p backup/ssh

if ! test -f ~/.ssh; then
    mkdir -p ~/.ssh
fi

if test -f ~/.bashprofile; then
    cp ~/.bash_profile backup/bash_profile
fi

cp -R ~/.ssh/* backup/ssh

cp -Rf src/* ~/.ssh
cat template > ~/.bash_profile
